# Force LF line endings on shell scripts and CI configs so they work on Linux runners
# regardless of who commits from which OS.
*.sh        text eol=lf
.githooks/* text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
