---
import { getServices, getMainCity, getMainCitySlug, getPrimaryCategorySlug } from "@lib/cluster";

interface Props {
  title?: string;
  showAll?: boolean;
}

const { title = "Nuestros servicios", showAll = true } = Astro.props;
const services = getServices();
const mainCity = getMainCity();
const mainCitySlug = getMainCitySlug();
const primaryCatSlug = getPrimaryCategorySlug();
const list = showAll ? services : services.slice(0, 3);
---

<section class="services-grid">
  <h2 class="services-grid__heading">{title}</h2>

  <div class="services-grid__list">
    {list.map((s) => (
      <a href={`/${primaryCatSlug}/${mainCitySlug}/${s.slug}/`} class="service-card">
        <h3 class="service-card__title">{s.name}</h3>
        <p class="service-card__description">En {mainCity}, presupuesto sin compromiso.</p>
        <span class="service-card__cta">Ver detalles →</span>
      </a>
    ))}
  </div>
</section>
